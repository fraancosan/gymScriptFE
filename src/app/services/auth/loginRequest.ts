export interface LoginRequest {
  mail: string;
  contraseña: string;
}

/*export type Roles = 'admin' | 'user';

export interface UserReponse {
    id: number;
    email: string;
    roles: Roles[];
}*/
